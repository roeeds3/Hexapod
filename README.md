# Hexapod
An Arduino IDE Compatible library for controlling a Hexapod with 6 3DOF legs using inverse kinematics. the parameters for the hexapod are configurable by editing the HexapodConfig File or by creating a config object and initializing the hexapod with it.
